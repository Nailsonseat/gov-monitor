services:
  # 1. The ETL Ingestion Daemon (Now disguised as a web service)
  - type: web
    name: gov-monitor-pipeline
    env: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false 
      - key: DB_CA_CERT
        sync: false
      - key: DATA_GOV_IN_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: TARGET_CITY
        value: "Pune"
      - key: TARGET_DISTRICT
        value: "Pune"
      - key: LANGUAGE_TARGET
        value: "Marathi"

  # 2. The Metabase Dashboard
  - type: web
    name: gov-monitor-dashboard
    env: image
    image:
      url: docker.io/metabase/metabase:v0.62.2
    plan: free
    envVars:
      - key: MB_DB_TYPE
        value: "postgres"
      - key: MB_DB_CONNECTION_URI
        sync: false